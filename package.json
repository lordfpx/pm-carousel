{
  "name": "pm-carousel",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite preview",
    "build:main": "vite build --base=/pm-carousel/",
    "preversion": "npm run build:main && git add .",
    "postversion": "git push && git push --tags"
  },
  "devDependencies": {
    "prismjs": "^1.23.0",
    "sass": "^1.32.7",
    "vite": "^2.0.0"
  },
  "dependencies": {
    "tachyons": "^4.12.0"
  }
}
