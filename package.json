{
  "name": "pm-carousel",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite preview",
    "preversion": "vite build --base=/pm-carousel/ && git add .",
    "postversion": "git push && git push --tags"
  },
  "devDependencies": {
    "sass": "^1.32.7",
    "vite": "^2.0.0"
  },
  "dependencies": {
    "tachyons": "^4.12.0"
  }
}
